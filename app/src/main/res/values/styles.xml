<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/ultra_white</item>
        <item name="titleTextColor">@color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- style for tab-layout -->
    <style name="PopTabText">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="textAllCaps">true</item>
    </style>

    <!-- style for project data dialog -->
    <style name="TextAppearance" parent="Theme.AppCompat">
        <item name="android:textColorHint">@color/bluish</item>
        <item name="android:textColor">@color/bluish</item>
        <item name="android:textSize">16sp</item>
        <item name="colorAccent">@color/bluish</item>
        <item name="colorControlNormal">@color/bluish</item>
        <item name="colorControlActivated">@color/bluish</item>
    </style>

</resources>
